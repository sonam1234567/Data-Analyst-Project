/* group by, order by */
/*
select Gender, count(Gender)
from EmployeeDemography
where age> 31
group by gender
order by gender
*/

/*Order by is used to make the data in an ascending or descinding order 
select * from EmployeeDemography order by age, gender
 */
 
 /* The 4 and 5 represents the first 4 col and last 5th col. 
 select * from EmployeeDemography order by 4 desc, 5 asc */