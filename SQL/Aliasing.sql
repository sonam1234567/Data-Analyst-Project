/* Aliasing -- to change table name or column name in the script for temporary purpose.*/

-- select FirstName as fname from EmployeeDemography

-- select concat(FirstName,' ',lastName) As FullName from EmployeeDemography

-- select avg(age) as AverageAge from EmployeeDemography